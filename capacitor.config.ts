import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.isj.second',
  appName: 'I&P',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
